using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Left_TF : MonoBehaviour
{
    public GameObject t;
    //TrafficLightManager traffic;
   // int left = 0;//t was annoying because of the cursor to change right to front
   
    // Start is called before the first frame update
    private void Awake()
    {
      //  traffic = t.GetComponent<TrafficLightManager>();
    }



    // Update is called once per frame
    void Update()
    {
        
    }
}
