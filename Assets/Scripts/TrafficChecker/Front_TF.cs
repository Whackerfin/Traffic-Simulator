using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Front_TF : MonoBehaviour
{
    public GameObject t;
    // Start is called before the first frame update
    //TrafficLightManager traffic;
   /// int front = 2;
    private void Awake()
    {
      //  traffic = t.GetComponent<TrafficLightManager>();
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
